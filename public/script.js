// no script tag needed here

let age = 15
let maxAge = 100
let numPerDay = 2
totalRequired = (numPerDay * 365) * (maxAge - age); 

document.write("You will need " + totalRequired + " buckets of fried chicken to last you until the ripe old age of " + maxAge);